<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>My Free Website</title>
  <meta name="description" content="A clean, fast one‑page site you can publish for free.">
  <meta name="color-scheme" content="light dark">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg: #0b1220;          /* change to your brand color */
      --card: #0f172a;        /* slate-900 */
      --muted: #94a3b8;       /* slate-400 */
      --text: #e2e8f0;        /* slate-200 */
      --accent: #7c3aed;      /* violet-600 */
      --accent-2: #22d3ee;    /* cyan-400 */
      --ring: #a78bfa;        /* violet-300 */
      --maxw: 1100px;
      --shadow: 0 10px 30px rgba(0,0,0,.25);
      --radius: 18px;
    }
    *{box-sizing:border-box}
    html,body{padding:0;margin:0;font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial,sans-serif;background:linear-gradient(180deg,var(--bg),#0b1120 60%,#050816);color:var(--text)}
    a{color:var(--accent)}
    .container{max-width:var(--maxw);margin:0 auto;padding:0 1.2rem}
    header{position:sticky;top:0;backdrop-filter:saturate(180%) blur(10px);background:rgba(5,8,22,.6);border-bottom:1px solid rgba(148,163,184,.15);z-index:50}
    nav{display:flex;align-items:center;justify-content:space-between;padding:.9rem 0}
    .brand{display:flex;align-items:center;gap:.7rem;font-weight:800;letter-spacing:.4px}
    .brand .dot{width:12px;height:12px;border-radius:50%;background:linear-gradient(45deg,var(--accent),var(--accent-2));box-shadow:0 0 20px var(--accent)}
    .navlinks{display:flex;gap:1rem;align-items:center}
    .navlinks a{padding:.5rem .8rem;border-radius:999px;text-decoration:none;color:var(--text);opacity:.9}
    .cta{background:linear-gradient(135deg,var(--accent),var(--accent-2));color:#0a0a0a;font-weight:700;box-shadow:var(--shadow)}
    .hero{padding:5rem 0 3rem;display:grid;grid-template-columns:1.2fr .8fr;gap:2rem;align-items:center}
    .h-kicker{color:var(--accent-2);font-weight:700;letter-spacing:.12em;text-transform:uppercase}
    h1{font-size:clamp(2rem,5vw,3.3rem);line-height:1.1;margin:.3rem 0}
    .lead{font-size:1.1rem;color:var(--muted);max-width:60ch}
    .hero-card{background:linear-gradient(180deg,rgba(255,255,255,.04),rgba(255,255,255,.02));border:1px solid rgba(148,163,184,.18);border-radius:var(--radius);padding:1.2rem;box-shadow:var(--shadow)}
    .hero-card .stat{display:grid;grid-template-columns:auto 1fr;gap:.7rem;margin:.8rem 0;align-items:center}
    .chip{border:1px solid rgba(148,163,184,.25);padding:.35rem .6rem;border-radius:999px;font-size:.85rem;color:var(--muted)}
    .buttons{display:flex;gap:.7rem;flex-wrap:wrap;margin-top:1rem}
    .btn{border:1px solid rgba(148,163,184,.25);padding:.7rem 1rem;border-radius:12px;text-decoration:none;color:var(--text);display:inline-flex;align-items:center;gap:.5rem}
    .btn:hover{border-color:var(--ring)}
    section{padding:3.5rem 0;border-top:1px solid rgba(148,163,184,.1)}
    .cards{display:grid;grid-template-columns:repeat(3,1fr);gap:1rem}
    .card{background:linear-gradient(180deg,rgba(255,255,255,.04),rgba(255,255,255,.02));border:1px solid rgba(148,163,184,.18);border-radius:var(--radius);padding:1rem;box-shadow:var(--shadow)}
    .card h3{margin:.2rem 0}
    .grid-2{display:grid;grid-template-columns:1fr 1fr;gap:1rem}
    .gallery{display:grid;grid-template-columns:repeat(4,1fr);gap:.8rem}
    .tile{aspect-ratio:4/3;border-radius:14px;border:1px solid rgba(148,163,184,.18);background:radial-gradient(1200px 300px at 10% 10%,rgba(124,58,237,.35),transparent),linear-gradient(180deg,rgba(255,255,255,.02),rgba(255,255,255,.01));}
    .contact form{display:grid;gap:.8rem}
    input,textarea{width:100%;padding:.9rem;border-radius:12px;border:1px solid rgba(148,163,184,.25);background:#0b1020;color:var(--text)}
    input:focus,textarea:focus{outline:none;border-color:var(--ring);box-shadow:0 0 0 4px rgba(167,139,250,.15)}
    footer{padding:2rem 0;color:var(--muted);text-align:center}
    .notice{font-size:.85rem;color:var(--muted)}
    .badge{font-size:.75rem;background:#0b1020;border:1px solid rgba(148,163,184,.2);padding:.25rem .5rem;border-radius:999px}
    @media (max-width: 900px){.hero{grid-template-columns:1fr}.cards{grid-template-columns:1fr}.gallery{grid-template-columns:repeat(2,1fr)}.grid-2{grid-template-columns:1fr}}
  </style>
</head>
<body>
  <header>
    <nav class="container">
      <div class="brand"><span class="dot"></span> <span id="site-name">My Brand</span></div>
      <div class="navlinks">
        <a href="#about">About</a>
        <a href="#features">Features</a>
        <a href="#gallery">Gallery</a>
        <a href="#contact" class="cta">Contact</a>
      </div>
    </nav>
  </header>

  <main class="container">
    <!-- HERO -->
    <section class="hero" id="home">
      <div>
        <div class="h-kicker">Launch Fast — For Free</div>
        <h1 id="hero-title">A sleek one‑page website template</h1>
        <p class="lead" id="hero-lead">Edit the text, swap a few links, and you can deploy this site to the internet in minutes using GitHub Pages or Netlify — no coding required.</p>
        <div class="buttons">
          <a class="btn cta" href="#contact">Get in touch →</a>
          <a class="btn" href="#features">See features</a>
          <span class="badge">100% free & open source</span>
        </div>
      </div>
      <div class="hero-card" aria-label="Quick highlights">
        <div class="chip">What you get</div>
        <div class="stat"><span>⚡</span><div><strong>Fast</strong><div class="notice">Loads in under a second on most devices.</div></div></div>
        <div class="stat"><span>🧩</span><div><strong>Modular</strong><div class="notice">Remove sections you don't need, no breakage.</div></div></div>
        <div class="stat"><span>🔎</span><div><strong>SEO‑ready</strong><div class="notice">Semantic HTML, good metadata, accessible.</div></div></div>
      </div>
    </section>

    <!-- ABOUT -->
    <section id="about">
      <div class="grid-2">
        <div>
          <h2>About this template</h2>
          <p class="lead">This is a no‑frills starter: headings, text, cards, a gallery, and a contact form you can connect to any tool (Zapier, Airtable, Formspree, Google Forms).</p>
          <p>Change the brand colors in <code>:root</code>, update the copy, and you're live. If you want this to say <em>Velora</em> or any other brand, just edit the text below or ask your AI assistant to replace it automatically.</p>
        </div>
        <div class="card">
          <h3>Quick checklist</h3>
          <ul>
            <li>Update <strong>site name</strong> and <strong>title</strong></li>
            <li>Add your <strong>social links</strong></li>
            <li>Replace <strong>gallery tiles</strong> with images</li>
            <li>Connect <strong>contact form</strong></li>
          </ul>
        </div>
      </div>
    </section>

    <!-- FEATURES -->
    <section id="features">
      <h2>Features</h2>
      <div class="cards">
        <article class="card">
          <h3>Responsive layout</h3>
          <p>Looks great on phones, tablets, and desktops without extra effort.</p>
        </article>
        <article class="card">
          <h3>Accessible by default</h3>
          <p>Clean structure, sufficient contrast, and keyboard‑friendly controls.</p>
        </article>
        <article class="card">
          <h3>Easy theming</h3>
          <p>Tweak a few CSS variables to match your brand’s vibe.</p>
        </article>
      </div>
    </section>

    <!-- GALLERY -->
    <section id="gallery">
      <h2>Gallery</h2>
      <div class="gallery">
        <div class="tile" title="Add your image here"></div>
        <div class="tile" title="Add your image here"></div>
        <div class="tile" title="Add your image here"></div>
        <div class="tile" title="Add your image here"></div>
      </div>
      <p class="notice">Replace these tiles with real images by setting <code>background-image</code> in CSS or placing <code>&lt;img&gt;</code> tags.</p>
    </section>

    <!-- TESTIMONIALS / PRICING (optional) -->
    <section id="extras">
      <div class="grid-2">
        <div class="card">
          <h3>What people say</h3>
          <p>“Simple, fast, and it just works.” — Happy Human</p>
          <p>“I launched in an afternoon.” — Busy Founder</p>
        </div>
        <div class="card">
          <h3>Simple pricing</h3>
          <p><strong>Free</strong> — host on GitHub Pages or Netlify.</p>
          <p><strong>+ Custom domain</strong> — connect your own .com for ~€10/yr.</p>
        </div>
      </div>
    </section>

    <!-- CONTACT -->
    <section id="contact" class="contact">
      <h2>Contact</h2>
      <form name="contact" method="POST" data-netlify="true">
        <input type="hidden" name="form-name" value="contact" />
        <label>
          <span class="notice">Your name</span>
          <input name="name" placeholder="Jane Doe" required>
        </label>
        <label>
          <span class="notice">Email</span>
          <input type="email" name="email" placeholder="you@example.com" required>
        </label>
        <label>
          <span class="notice">Message</span>
          <textarea name="message" rows="5" placeholder="How can I help?" required></textarea>
        </label>
        <button class="btn cta" type="submit">Send message</button>
      </form>
      <p class="notice">Or email <a href="mailto:hello@example.com">hello@example.com</a></p>
    </section>
  </main>

  <footer>
    <div class="container">
      <p>© <span id="year"></span> <span id="brand">My Brand</span>. All rights reserved.</p>
      <p>
        <a href="#">Instagram</a> • <a href="#">TikTok</a> • <a href="#">YouTube</a>
      </p>
    </div>
  </footer>

  <script>
    // Small niceties
    document.getElementById('year').textContent = new Date().getFullYear();
    // Optional: quick rename helper — change these once
    const BRAND = 'My Brand';
    document.getElementById('site-name').textContent = BRAND;
    document.getElementById('brand').textContent = BRAND;
    document.getElementById('hero-title').textContent = 'Stand out with ' + BRAND;
  </script>
</body>
</html>
